{
  "name": "phoenix",
  "version": "2.6.3",
  "private": true,
  "devDependencies": {
    "jscs": "3.0.7",
    "jshint": "2.9.7",
    "uglify-js": "3.4.9"
  },
  "scripts": {
    "build": "npm run clean && npm test && cat library/src/*.js > Phoenix/$npm_package_name.js && uglifyjs Phoenix/$npm_package_name.js --compress --mangle --output Phoenix/$npm_package_name.min.js",
    "clean": "rm -frv Phoenix/$npm_package_name.js Phoenix/$npm_package_name.min.js",
    "lint": "jshint --config jshint.json library/ && jscs --config jscs.json library/",
    "test": "npm run lint"
  },
  "engines": {
    "node": ">=11.5.0",
    "npm": ">=6.5.0"
  }
}
